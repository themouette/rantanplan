{
  "name": "rantanplan",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.1"
  },
  "proxy": "http://localhost:1337",
  "scripts": {
    "dev": "npm run start",
    "start": "npm-run-all -p watch-flow watch-js",
    "build": "react-scripts build",
    "watch-flow": "chokidar 'src/**/*.js' -c 'npm run flow'",
    "watch-js": "react-scripts start",
    "flow": "flow",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "chokidar-cli": "^1.2.0",
    "flow-bin": "^0.69.0",
    "npm-run-all": "^4.1.2"
  }
}
